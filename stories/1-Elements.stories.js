import * as d3 from 'd3';
import React from 'react';
import {} from 'lodash/fp';
import { Node, Wrapper, El, Els } from '../src';

export const Basic = () => (
    <div
        style={{
            position: 'relative',
            width: '100%',
            maxWidth: '800px',
            margin: 'auto'
        }}
    >
        <h3>Th</h3>
    </div>
);
