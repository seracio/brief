import _ from 'lodash/fp';
import React from 'react';
import { recycleConnect } from './recycle';

type Props = {};

class Axis extends React.PureComponent {
    render() {
        return null;
    }
}

export default recycleConnect()(Axis);
