import * as base from './base';
import * as extra from './extra';

export const {
    FulgurContext,
    El,
    Els,
    Map,
    Node,
    XAxis,
    YAxis,
    Circles,
    Bins
} = {
    ...base,
    ...extra
};
